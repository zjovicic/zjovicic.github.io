#include <stdio.h>
#include <stdlib.h>

int issamog (char a);

    // UTROVACKI
int main() {
    printf("Unesi rijec:\n");
    char *rijec = malloc(30);
    scanf("%s",rijec);
    char *pocetak = malloc(30);
    char *kraj = malloc(30);
    int dio = 1;
    int brojac = 0;
    int i = 0;
    
    while (rijec[i] != '\0')
    {
        if (dio == 1 && !(issamog(rijec[i])) )
        {
            pocetak[i] = rijec[i];
        }
        else if (dio == 1 && issamog(rijec[i]))
        {
            pocetak[i] = rijec[i];
            dio = 2;
            brojac = i;
        }
        else if (dio == 2)
        {
         kraj[i-brojac-1] = rijec[i];   
        }
        i++;
    }
    
    printf("To se na utrovackom kaze u%sza%snje.\n", kraj, pocetak);

    free(rijec);
    free(pocetak);
    free(kraj);
    return 0;
}

int issamog (char a)
{
    int result = 0;
    
    if (a == 65 || a == 65 + 32)
    {
        result = 1;
    }
        if (a == 69 || a == 69 + 32)
    {
        result = 1;
    }
        if (a == 73 || a == 73 + 32)
    {
        result = 1;
    }
        if (a == 79 || a == 79 + 32)
    {
        result = 1;
    }
        if (a == 85 || a == 85 + 32)
    {
        result = 1;
    }
    return result;
}